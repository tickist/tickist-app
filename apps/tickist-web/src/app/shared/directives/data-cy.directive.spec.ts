import { DataCyDirective } from './data-cy.directive';

describe('DataCyDirective', () => {
  it('should create an instance', () => {
    const directive = new DataCyDirective();
    expect(directive).toBeTruthy();
  });
});
