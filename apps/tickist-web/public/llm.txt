# Tickist

> Tickist is an open-source task and project workspace built with Angular and Supabase. It helps people collect work in an Inbox, organize it into projects, plan deadlines and reminders, and focus on the next useful action.

## Product summary

Tickist provides an Inbox, nested projects, tasks, tags, subtasks, recurring work, reminders, collaboration, dashboards, statistics, notifications, backup/restore, and API tokens. Project branding includes a searchable catalogue of 151 Lucide icons, and placeholders use a consistent muted slate colour throughout forms. Tasks may be suspended indefinitely or until a selected date and time; the time defaults to `00:00` when only the date is chosen. Suspended work has its own project filter, the project-header suspended count opens that filter, and suspended tasks are excluded from active-work summaries until they resume. Selecting a project includes tasks from all of its nested subprojects by default, with per-project checkboxes for temporarily narrowing that hierarchy. Task editing shows creation and latest modification timestamps, and only one task-card actions menu remains open within a list. Shared-project assignees are shown by display label, and new shared tasks default to the person creating them. It is a private workspace application: project, task, tag, team, settings, and API routes require an authenticated user.

Repository operators can generate a deterministic English demo account with 30 projects and 200 tasks under strict Auth-marker and project-confirmation guards. They can also create locally encrypted, authenticated Supabase disaster-recovery archives containing database, Auth, migration, Storage metadata, and physical Storage objects.

Authenticated integrations use `https://mcp.tickist.com/mcp` for project, task, and tag operations. The official SDK server supports MCP `2026-07-28`, retains `2025-06-18` compatibility, and accepts OAuth 2.1 PKCE tokens or scoped personal Bearer tokens. OAuth requests use the user's Supabase token and RLS; the MCP Worker has no service-role credential. The old `https://tickist.com/mcp` address is a temporary proxy.

## Canonical knowledge

- Full product and technical reference: [llm-full.txt](/llm-full.txt)
- Feature reference for repository-aware tools: [product features](https://github.com/tickist/tickist-app/blob/develop/doc/product-features.md)
- Architecture and data boundaries: [architecture](https://github.com/tickist/tickist-app/blob/develop/doc/architecture.md)
- Server-side automation: [Edge Functions](https://github.com/tickist/tickist-app/blob/develop/doc/edge-functions.md)
- Operations, local development, testing, and deployment: [operations](https://github.com/tickist/tickist-app/blob/develop/doc/operations.md)
- Public web and crawler policy: [public content](https://github.com/tickist/tickist-app/blob/develop/doc/public-content.md)
- Contributor instructions: [AGENTS.md](https://github.com/tickist/tickist-app/blob/develop/AGENTS.md)

## Public web surface

- Canonical public pages: `https://tickist.com/`, `https://tickist.com/en/blog`, and `https://tickist.com/pl/blog`.
- The public blog is responsive, requires no sign-in, and has no comments, CMS, or admin panel.
- Blog content is code-reviewed Markdown in `apps/tickist-web/content/blog/`. English and Polish have independent article/category/tag collections; do not infer translations or link records automatically.
- Draft Markdown is excluded from public output. Published articles are generated into locale article routes, category/tag navigation, RSS, sitemap entries, and sanitized HTML with canonical, Open Graph, Twitter-card, and JSON-LD metadata.
- The Cloudflare Worker injects generated blog metadata into initial SPA HTML; Angular mirrors it after client navigation.
- Sitemap: `https://tickist.com/sitemap.xml`
- Crawler policy: `https://tickist.com/robots.txt`
- Authentication and user workspace routes are intentionally not public content.
- The landing page links to `https://spendist.app/`, a related open-source application for tracking spending, recurring payments, and personal finances.

## Technology

- Angular 21 standalone application with signals, Vite, Tailwind CSS, and DaisyUI
- Supabase Postgres, Auth, Storage, Realtime, Row Level Security, and Edge Functions
- Nx workspace with Vitest and Playwright
- Cloudflare Worker deployment and AWS SES email delivery

## Maintenance rule

When a product capability changes, update the matching document in `doc/`, this concise file, and `llm-full.txt`. When a new public route is added, also update `sitemap.xml` and `robots.txt`.
